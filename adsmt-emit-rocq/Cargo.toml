[package]
name = "adsmt-emit-rocq"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Rocq (Coq) backend for adsmt-cert certificates. Mirrors the Lean emit shape exactly; Ltac1 excluded — Ltac2 only."
repository = "https://github.com/newsniper-org/adsmt-contrib"

[dependencies]
adsmt-cert = { workspace = true }
adsmt-core = { workspace = true }
